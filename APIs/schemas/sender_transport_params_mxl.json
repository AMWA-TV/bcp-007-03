{
  "$schema": "http://json-schema.org/draft/2019-09/schema#",
  "description": "Describes MXL transport parameters. The constraints in this schema are minimum constraints, but may be further constrained at the constraints endpoint. Senders MUST support at least the `mxl_domain_id` and `mxl_flow_id` parameters.",
  "title": "MXL Sender Transport Parameters",
  "type": "object",
  "properties": {
    "mxl_domain_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Specifies the MXL domain id where the MXL flow will be located. A null value indicates that the domain ID has not been configured.",
      "anyOf": [
        {
          "format": "uuid"
        },
        {
          "type": "null"
        }
      ]
    },
    "mxl_flow_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Specifies the MXL flow id which will be used for write operation. A null value indicates that the flow ID has not been configured.",
      "anyOf": [
        {
          "format": "uuid"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "patternProperties": {
    "^ext_[a-zA-Z0-9_]+$": {
      "$ref": "sender_transport_params_ext.json"
    }
  },
  "additionalProperties": false
}